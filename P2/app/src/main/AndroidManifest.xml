<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.p2">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".frontend.AboutActivity"></activity>
        <activity android:name=".frontend.ResultActivity" />
        <activity android:name=".frontend.ResultTextActivity" />
        <activity android:name=".frontend.TestActivity" />
        <activity android:name=".frontend.MainActivity" />
        <activity android:name=".frontend.InstructionActivity" />
        <activity android:name=".frontend.StartActivity">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
    </application>
</manifest>